import {Delete01Icon} from 'hugeicons-react-native'

const Delete = (props) => {

  return (
    <Delete01Icon
      size={24} 
      variant={'stroke'}
      color={props.color}
      strokeWidth={props.strokeWidth}
    />
  )
}
  
export default Delete;
  