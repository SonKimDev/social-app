import {MailSend01Icon} from 'hugeicons-react-native'

const Send = (props) => {

  return (
    <MailSend01Icon
      size={24} 
      variant={'stroke'}
      color={props.color}
      strokeWidth={props.strokeWidth}
    />
  )
}
  
export default Send;
  