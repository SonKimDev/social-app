import {Edit01Icon} from 'hugeicons-react-native'

const Edit = (props) => {

  return (
    <Edit01Icon
      size={24} 
      variant={'stroke'}
      color={props.color}
      strokeWidth={props.strokeWidth}
    />
  )
}
  
export default Edit;
  